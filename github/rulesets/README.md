# Ruleset JSON Files

このディレクトリには GitHub Rulesets の export JSON を置く。

運用ルール:

- reference repo から export した JSON をそのまま保存する
- 何に使う ruleset か分かる名前にする
- repo 固有の required checks や bypass actor は import 後に確認する

詳細手順は `github/docs/github-rulesets.md` を参照。
