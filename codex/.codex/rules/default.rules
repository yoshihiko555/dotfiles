# Global guardrails for commands that request execution outside the sandbox.

prefix_rule(
    pattern = [["bash", "sh", "zsh"], ["-lc", "-c"]],
    decision = "prompt",
    justification = "Shell wrappers can hide multiple actions; require approval outside the sandbox.",
    match = [
        "bash -lc npm test",
        "zsh -c git status",
    ],
    not_match = [
        "bash --version",
        "zsh --version",
    ],
)

prefix_rule(
    pattern = ["sudo"],
    decision = "prompt",
    justification = "Privilege escalation requires explicit approval.",
    match = [
        "sudo apt install ripgrep",
        "sudo systemctl restart docker",
    ],
)

prefix_rule(
    pattern = ["rm"],
    decision = "prompt",
    justification = "Deleting files outside the sandbox requires explicit approval.",
    match = [
        "rm file.txt",
        "rm -rf build",
    ],
)

prefix_rule(
    pattern = ["rmdir"],
    decision = "prompt",
    justification = "Deleting directories outside the sandbox requires explicit approval.",
    match = [
        "rmdir old-dir",
    ],
)

prefix_rule(
    pattern = ["git", "reset", "--hard"],
    decision = "prompt",
    justification = "Hard resets discard work and require explicit approval.",
    match = [
        "git reset --hard",
        "git reset --hard HEAD~1",
    ],
    not_match = [
        "git reset HEAD file.txt",
    ],
)

prefix_rule(
    pattern = ["git", "clean"],
    decision = "prompt",
    justification = "Git clean deletes untracked files and requires explicit approval.",
    match = [
        "git clean -fd",
        "git clean -fdx",
    ],
)

prefix_rule(
    pattern = ["git", "push"],
    decision = "prompt",
    justification = "Pushing changes is a remote side effect and requires explicit approval.",
    match = [
        "git push",
        "git push origin main",
    ],
)

prefix_rule(
    pattern = ["gh", "pr", ["merge", "close"]],
    decision = "prompt",
    justification = "Changing pull request state requires explicit approval.",
    match = [
        "gh pr merge 123",
        "gh pr close 123",
    ],
    not_match = [
        "gh pr view 123",
    ],
)

prefix_rule(
    pattern = [["npm", "pnpm", "yarn"], ["install", "add", "remove", "upgrade"]],
    decision = "prompt",
    justification = "Dependency changes require explicit approval outside the sandbox.",
    match = [
        "npm install",
        "pnpm add vite",
        "yarn remove react",
    ],
    not_match = [
        "npm test",
        "pnpm lint",
    ],
)

prefix_rule(
    pattern = [["pip", "pip3", "uv"], ["install", "add", "remove"]],
    decision = "prompt",
    justification = "Dependency changes require explicit approval outside the sandbox.",
    match = [
        "pip install requests",
        "pip3 install pytest",
        "uv add ruff",
    ],
    not_match = [
        "uv run pytest",
    ],
)

prefix_rule(
    pattern = [["brew", "cargo", "go"], "install"],
    decision = "prompt",
    justification = "Tool installation requires explicit approval.",
    match = [
        "brew install ripgrep",
        "cargo install cargo-nextest",
        "go install example.com/tool@latest",
    ],
)

# Codex セッションで個別に許可したコマンド。
prefix_rule(pattern=["awk", "NR>=1&&NR<=260{print NR\":\"$0}", "configs/openapi.yml"], decision="allow")
prefix_rule(pattern=["awk", "NR>=260&&NR<=520{print NR\":\"$0}", "configs/openapi.yml"], decision="allow")
prefix_rule(pattern=["awk", "NR>=520&&NR<=1040{print NR\":\"$0}", "configs/openapi.yml"], decision="allow")
prefix_rule(pattern=["awk", "NR>=1040&&NR<=1560{print NR\":\"$0}", "configs/openapi.yml"], decision="allow")
prefix_rule(pattern=["docker", "compose", "up", "-d", "api"], decision="allow")
prefix_rule(pattern=["node", "-e", "const fs=require('fs'); new Function(fs.readFileSync('apps/chatgpt/monthlyReview.js','utf8')); console.log('OK');"], decision="allow")
prefix_rule(pattern=["node", "-e", "const fs=require('fs');const d=JSON.parse(fs.readFileSync('02_Monthly/monthly_review_2025-01.json','utf8'));console.log(Object.keys(d.llm||{}));console.log(JSON.stringify(d.llm,null,2).slice(0,400));"], decision="allow")
prefix_rule(pattern=["node", "-e", "const fs=require('fs');const d=JSON.parse(fs.readFileSync('03_Year/annual_review_2025.json','utf8'));console.log(Object.keys(d));console.log(Object.keys(d.llm||{}));"], decision="allow")
prefix_rule(pattern=["node", "-e", "const fs=require('fs');const d=JSON.parse(fs.readFileSync('03_Year/annual_review_2025.json','utf8'));console.log(JSON.stringify(d.annual,null,2).slice(0,800));"], decision="allow")
prefix_rule(pattern=["node", "-e", "const fs=require('fs');const d=JSON.parse(fs.readFileSync('02_Monthly/monthly_review_2025-01.json','utf8'));console.log(Object.keys(d.monthly));console.log(Object.keys(d.monthly.tasks||{}));console.log(Object.keys(d.monthly.summary||{}));"], decision="allow")
prefix_rule(pattern=["node", "-e", "const fs=require('fs');const d=JSON.parse(fs.readFileSync('02_Monthly/monthly_review_2025-01.json','utf8'));console.log(JSON.stringify(d.stats,null,2));"], decision="allow")
prefix_rule(pattern=["/bin/zsh", "-lc", "python3 - <<'PY'\nimport json, pathlib, itertools\np=pathlib.Path('/Users/yoshihiko/Library/CloudStorage/GoogleDrive-yoshihiko05410@gmail.com/マイドライブ/01_Automation/02_GAS/02_JSON/02_Monthly/monthly_review_2025-11.json')\nobj=json.loads(p.read_text())\nmonthly=obj.get('monthly',{})\nprint(monthly.keys())\nprint('summary', monthly.get('summary',{}))\nprint('task keys', monthly.get('task',{}).keys())\nprint('projects', len(monthly.get('projects',[])))\nprint('reflections', len(monthly.get('reflections',[])))\nPY"], decision="allow")
prefix_rule(pattern=["docker", "run", "--rm", "node:20-alpine", "sh", "-lc", "npm view postcss-nesting@10.1.10 engines peerDependencies"], decision="allow")
prefix_rule(pattern=["docker", "run", "--rm", "node:20-alpine", "sh", "-lc", "npm view @nuxtjs/tailwindcss version dependencies"], decision="allow")
prefix_rule(pattern=["mkdir", "-p", "frontend/src/tests/features/auth/screens"], decision="allow")
prefix_rule(pattern=["npm", "test"], decision="allow")
prefix_rule(pattern=["git", "add", "docs/08_adr/ADR-20260131-019.md", "docs/08_adr/DECISIONS.md"], decision="allow")
prefix_rule(pattern=["diff", "-u", "claude-code-orchestra-main/.claude/hooks/agent-router.py", "hooks/agent-router.py"], decision="allow")
prefix_rule(pattern=["diff", "-u", "claude-code-orchestra-main/.claude/hooks/log-cli-tools.py", "hooks/log-cli-tools.py"], decision="allow")
prefix_rule(pattern=["diff", "-u", "claude-code-orchestra-main/.claude/hooks/suggest-gemini-research.py", "hooks/suggest-gemini-research.py"], decision="allow")
prefix_rule(pattern=["sort"], decision="allow")
prefix_rule(pattern=["git", "add", "packages/core/skills/plan/SKILL.md"], decision="allow")
prefix_rule(pattern=["swift", "test"], decision="allow")
prefix_rule(pattern=["git", "add"], decision="allow")
prefix_rule(pattern=["gh", "issue", "view"], decision="allow")
prefix_rule(pattern=["go", "test"], decision="allow")
prefix_rule(pattern=["go", "vet"], decision="allow")
prefix_rule(pattern=["printf", "deny-check\\n"], decision="allow")
