# herdr-terminal-notifier の設定。~/.config/herdr/plugins/config/dot.terminal-notifier/config.env へ
# mkLink で配線する（既定値は プラグイン の lib/config.sh）。

# 通知音は herdr 本体の [ui.sound] が鳴らすので、プラグイン側では鳴らさない
SOUND_BLOCKED=none
SOUND_DONE=none
