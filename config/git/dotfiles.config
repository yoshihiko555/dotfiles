# dotfiles リポジトリでのみ適用する git 設定。
# config（グローバル）から includeIf gitdir で読み込まれる。
[core]
	# 整形崩れだけで CI が落ちるのを防ぐ pre-commit フックを有効化する。
	# 相対パスにしているのは、git worktree からコミットした場合に
	# その worktree 側の .githooks/ を参照させるため
	# （フックは常に作業ツリーのトップで実行される）。
	hooksPath = .githooks
